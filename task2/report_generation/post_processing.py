from utils.pdf_utils import add_bookmarks

def add_bookmarks_to_pdf(input_pdf_path, output_pdf_path):
    """Adds bookmarks to the generated PDF."""
    # Assuming a utility function add_bookmarks from pdf_utils.py
    add_bookmarks(input_pdf_path, output_pdf_path)
    print(f"Bookmarks added to the PDF: {output_pdf_path}")


