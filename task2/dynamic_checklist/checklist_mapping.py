def map_content_to_section(section, content):
    """Maps the content to the appropriate section in the report."""
    # This can be more complex if mapping rules are needed
    section['content'] = content
    return section
